<h1> 商品詳細画面（ユーザ側）</h1>

<%= @product.cd_title %>

<div>
    <%= link_to product_carts_path(@product), method: :post do %>
    <div>カートに入れる</div>
    <% end %>
</div>

<%= link_to "商品一覧へ", products_path %>